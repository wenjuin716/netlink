1. Execute make first at root directory.
3. Enter kernel directory and insert kernel module by :$ sudo insmod netlinkKernel.ko      
4. Enter userspace directory and run ./netlinkUser to see message and run dmesg to see debug messages
5. Remove module by : $ sudo rmmod netlinkKernel          
6. Finally make clean to remove output files.

PS. Multicast Netlink should define NETLINK_MULTICAST
